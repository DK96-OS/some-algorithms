package maxdistance.data;

/** An interface for a provider of data to the algorithm.
 */
public interface AlgorithmDataProvider {

	/** Obtain the Array from the Provider.
	 * @return An Array.
	 */
	int[] getArray();

}
